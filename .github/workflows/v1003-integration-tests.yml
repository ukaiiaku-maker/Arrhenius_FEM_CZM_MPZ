name: v10.0.3 integration tests

on:
  push:
    branches:
      - v10.0.3-progressive-integration-audit
  pull_request:
    branches:
      - v10.0.2-pf-equivalent-event-lifecycle
    paths:
      - "arrhenius_fracture/**"
      - "tests/**"
      - "audit_v10_0_3_progressive_integration.py"
      - "run_v10_0_3_integration_tests.sh"
      - "pyproject.toml"
      - ".github/workflows/v1003-integration-tests.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  tests-only:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e .
          python -m pip install pytest

      - name: Run tests-only integration gate
        id: integration_tests
        env:
          CONDA_DEFAULT_ENV: arrhenius-fem-czm
          PYTHON_BIN: python
        shell: bash
        run: |
          set +e
          set -o pipefail
          bash run_v10_0_3_integration_tests.sh 2>&1 | tee v1003-integration-tests.log
          status=${PIPESTATUS[0]}
          echo "exit_status=$status" >> "$GITHUB_OUTPUT"
          exit "$status"

      - name: Upload complete test log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: v1003-integration-tests-log
          path: v1003-integration-tests.log
          if-no-files-found: error
