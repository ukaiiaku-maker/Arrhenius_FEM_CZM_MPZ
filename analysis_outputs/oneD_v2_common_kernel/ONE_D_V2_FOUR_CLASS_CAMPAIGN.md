# One D V2 Four Class Campaign

The 40-case 100-µm and 40-case 1000-µm campaigns were not launched because replay and forward-map gates did not pass. Empty V2 ledgers carry availability columns; no unsupported values are zero-filled.
